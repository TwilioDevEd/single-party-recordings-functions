{
  "name": "single-party-recordings",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "axios": "^1.6.4",
    "handlebars": "^4.7.6",
    "querystring": "^0.2.0",
    "twilio": "^4.19.3"
  },
  "devDependencies": {
    "configure-env": "^1.0.0",
    "twilio-run": "^2.6.0"
  },
  "engines": {
    "node": "10"
  }
}
